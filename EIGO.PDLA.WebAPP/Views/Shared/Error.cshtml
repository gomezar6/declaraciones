@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Error.</h1>
<h2 class="text-danger">Ocurrió un error al procesar su solicitud.</h2>

@if (Model?.ShowRequestId ?? false)
{
    <p>
       
    </p>
}

@if (Model?.ShowErrorMsg ?? false)
{
    <p>
        <strong>   @Html.Raw(@Model?.ErrorMsg)</strong>
    </p>
}

<p>
    Por favor espere unos minutos e intente nuevo. Si el problema persiste, póngase en contacto con soporte.
</p>
